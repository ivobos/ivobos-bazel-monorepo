# Test OTel code-based integration for JavaScript

The app sends telemetry to console
Start with
```sh
bazel run otel/code-based:app
```
and then you can generate more data with http://localhost:8080/rolldice
